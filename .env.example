# =============================================================================
# RedAmon — Environment Configuration
# =============================================================================
# Copy this file to .env and fill in at least one AI provider key.
#   cp .env.example .env
#
# All values below have sane defaults — you only NEED to set an AI key.
# =============================================================================

# ---------------------------------------------------------------------------
# AI Provider Keys (at least one required)
# ---------------------------------------------------------------------------
ANTHROPIC_API_KEY=              # recommended
OPENAI_API_KEY=

# ---------------------------------------------------------------------------
# Anthropic Base URL (optional — for proxies like copilot-api)
# When set, ANTHROPIC_API_KEY can be omitted (defaults to 'dummy').
# ---------------------------------------------------------------------------
# ANTHROPIC_BASE_URL=http://localhost:4141 or http://host.docker.internal:4141
# LLM_MODEL=claude-opus-4.6        # model name (default: gpt-5.2)

# ---------------------------------------------------------------------------
# Optional API Keys
# ---------------------------------------------------------------------------
TAVILY_API_KEY=                 # web search for AI agent
NVD_API_KEY=                    # NIST NVD (higher rate limits for vuln lookups)
